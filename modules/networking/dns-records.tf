# Este arquivo agora está obsoleto, pois usamos IngressRoutes no módulo Traefik.
# Mantido apenas para evitar que o Terraform quebre caso ainda referencie o módulo,
# mas todo o código de Ingress foi removido.
